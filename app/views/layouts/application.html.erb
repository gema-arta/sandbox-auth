<!DOCTYPE html>
<html>
<head>
  <title>SandboxAuth</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<% if user_signed_in? %>
<%= image_tag current_user.image , alt: "user image" %> Hi! Signed in as <%= current_user.first_name %>. We think your email is <%= current_user.email %> Not you?
<%= link_to "Sign out", '/sign_out', :as => :destroy_user_session %>

<% else %>
<%= link_to "Sign in with Facebook", user_omniauth_authorize_path(:facebook) %>
<%= link_to "Sign in with Google", user_omniauth_authorize_path(:google_oauth2) %>

<% end %>

<p>
  <% flash.each do |type, message| %>
    <div class=<%= type %>><%= message %></><br>
  <% end %>
</p>

<%= yield %>

</body>
</html>
